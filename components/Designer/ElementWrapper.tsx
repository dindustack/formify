import React from "react";
import { FormElementInstance } from "../Form/Elements";

export function DesignerElementWrapper({
  element,
}: {
  element: FormElementInstance;
}) {
    
  return <div>ElementWrapper</div>;
}
